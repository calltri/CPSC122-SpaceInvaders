#pragma once

#include<iostream>
#include<string>

int const WINDOW_WIDTH = 640;
int const WINDOW_HEIGHT = 800;

const float LASER_WIDTH = WINDOW_WIDTH / 320;
const float LASER_HEIGHT = WINDOW_HEIGHT / 40;
const float LASER_SPEED = WINDOW_HEIGHT / 50;

const float ENEMY_BASIC_WIDTH = 30;
const float ENEMY_BASIC_HEIGHT = 30;